# Relational Document Database
- always operates in terms of JSON objects, but using relational model
  - join / aggregates / ... transform shape of final set of JSON objects representing rows
- data stored as proper tables, but with unified types between SQL and JSON
- queries abstracted using query builder to iron out differences between underlying SQLite and our model

## Datatypes problem
- SQLite
  - `null`
  - `i64`
    - `boolean`
  - `f64`
  - `String`
    - `json`
  - `Vec<u8>` (blob)
    - `jsonb`
- JSON
  - `null`
  - `number`
  - `string`
  - `object`
  - `array`
  - `boolean`
- for top-level columns, we need schema to distinguish different (JSON) types with same (SQLite) representation
  - we need some internal table to track schemas of user-defined tables

## ML
- embeddings for semantic search
- vector DB? SQlite has vector extension available (https://github.com/asg017/sqlite-vec)
- https://atlas.nomic.ai/
- obsidian graph view
- contextual templates (suggest relevant template of table based on the note's initial content or purpose)
- highlight or extract tasks, deadlines, or decisions from meeting notes

## Query language
- select, join, union, aggregates, views, fts, vector search, nested json operations, ...

## Architecture
- DB
  - library embedded in main process
- ML
  - separate process (in Python)
  - Frontend -> Python API (request for ...)
  - Server-Sent Events (SSE) -> stream to frontend
  - https://developer.mozilla.org/en-US/docs/Web/API/Server-sent_events
  - https://tinfoil.sh/
  
FREE LICENSE!